# 异世界娼馆模拟器 (Otherworld Brothel Simulator)

> 一个由 AI 驱动剧情、深度 RPG 模拟经营的异世界 Web 游戏。

## 📅 项目进度 (Development Status)

**当前阶段：Core Loop & Systems V1 (核心循环与系统 V1)**

### ✨ 近期更新 (Recent Updates)

#### 1. 核心玩法闭环 (Core Loop Completed) - *New*
- **招募系统 (Recruitment)**: 
  - 实现了基于权重的随机 NPC 生成工厂 (`girlFactory.ts`)。
  - 新增塔罗牌风格的招募面板 (`RecruitPanel`)，包含 N/R/SR/SSR 稀有度体系。
- **日程系统 (Schedule)**:
  - 实现了早/中/晚三时段的任务分配逻辑。
  - 包含精力消耗、理智变化和金币收益的自动结算体系。
- **持久化存档**: 重构了存档机制，完整支持 `Game`, `Girls`, `Settings` 三大状态库的保存与读取。

#### 2. UI/UX 魔法化升级 - *New*
- **Magic Style**: 全面应用了“魔法/暗黑”视觉风格，引入了光晕 (Glow)、流光 (Shimmer) 和毛玻璃 (Glassmorphism) 效果。
- **交互优化**: 
  - `BaseButton`: 增加了点击反馈和悬停流光。
  - `BasePanel`: 增强了边框装饰和进场动画 (`modal-pop`)。
- **移动端优先**: 优化了弹窗布局，适配触摸操作。

#### 3. 工程化标准
- **TypeScript**: 新增模块 (`schedule.ts`, `types/`) 全面采用 TypeScript 开发。
- **Vite 配置**: 修复了 TS 配置红线，优化了构建配置。

---

## 🏗️ 核心架构 (Architecture)

### 技术栈
- **Frontend**: Vue 3 (Composition API) + Vite
- **State Management**: Pinia (TypeScript Supported)
- **UI Framework**: Custom CSS (Magic/Dark Theme) / Phosphor Icons
- **AI Layer**: Fetch API + Custom Protocol Middleware
- **Language**: Hybrid (Migrating to TypeScript)

### 目录结构
```
src/
├── api/
│   ├── llm.js         # AI 接口
│   └── save.js        # [Refactored] 存档管理
├── components/
│   ├── Base/          # [Updated] BaseButton, BasePanel
│   ├── Game/          # 
│   │   ├── Panel/     # [New] RecruitPanel, SchedulePanel
│   │   └── ...
├── stores/
│   ├── game.js        # 游戏全局状态
│   ├── girls.ts       # [TS] NPC 数据模型
│   ├── schedule.ts    # [TS] [New] 日程管理与结算
│   └── settings.js    # 系统设置
└── types/             # [TS] [New] 类型定义 (schedule.ts)
```

---

## 🚀 后续开发路线 (Roadmap)

### Phase 3: 灵魂注入 (Next Step)
- [ ] **AI 记忆系统 (Memory)**: 引入 Vector/Keyword 索引，让 AI 记住 NPC 的经历。
- [ ] **事件扩展**: 增加“特殊客人”、“突发事件”、“道具交互”等丰富日程的内容。
- [ ] **地图探索**: 实装 10x10 网格地图的点击移动与随机事件触发。

---

## 📝 开发者笔记
*   **TypeScript**: 新功能必须使用 TS 开发。旧 JS 文件暂不强制迁移，除非涉及重大重构。
*   **UI 规范**: 保持 `src/styles/variables.css` 中的魔法风格变量统一，优先使用 `BasePanel` 和 `BaseButton`。
